"""takflow contracts (``spec/``).

Holds the language-neutral contract artifacts that other tools vendor:

- ``jobspec/`` — the job-run resource description (``jobspec.schema.json`` +
  ``VERSION``), consumed by the Go ``orvix`` project.
- ``workflow/`` — (later) the generic workflow-config schema.
"""
